#include <bits/stdc++.h>
using namespace std;
int gcd(int a,int b)
{
      if (a == 0)
       return b;
    if (b == 0)
       return a;
  

    if (a == b)
        return a;
  
    if (a > b)
        return gcd(a-b, b);
    return gcd(a, b-a);
}
void solve()
{
    long long n;
    cin>>n;
int  j=0'
    for(int i=1;i<=n;i++)
    {
        if()
    }

}
int main()
{

int t;
cin>>t;
while(t--)
{
    solve();
}
    return 0;
}